# Global MMITSS Configuration
# Set this to the root directory of the MMITSS repository
# This variable is used by docker-compose.yml for volume mounts
MMITSS_ROOT=/home/norris/Documents/mmitss-az

# Network adapter for MACVLAN mode (if using macvlan driver)
# Change this to match your physical network interface (e.g., eth0, eno1, etc.)
MMITSS_NETWORK_ADAPTER=enp0s3

# Docker processor architecture (matches the image tag)
# Common values: amd64, arm64, armhf
PROCESSOR=amd64
